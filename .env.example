# Transport Mode Configuration
# Set to "http" for HTTP/SSE transport with bearer token auth
# Set to "stdio" (or leave unset) for STDIO transport (default)
TRANSPORT_MODE=stdio

# HTTP Server Configuration (only used when TRANSPORT_MODE=http)
HTTP_PORT=3000

# Bearer Token Authentication (only used when TRANSPORT_MODE=http)
# IMPORTANT: Set a strong random token in production!
# Generate with: openssl rand -base64 32
MCP_BEARER_TOKEN=

# Medusa Backend Configuration
MEDUSA_BACKEND_URL=http://localhost:9000
MEDUSA_USERNAME=admin@medusa-test.com
MEDUSA_PASSWORD=supersecret

# Medusa Store Configuration
PUBLISHABLE_KEY=

# Node Environment
NODE_ENV=development
